const CompanyController = require('./company.controller');

// quick note es6 we can write CompanyController:CompanyController or just CompanyController as key and value are the same
module.exports = {
  CompanyController
}